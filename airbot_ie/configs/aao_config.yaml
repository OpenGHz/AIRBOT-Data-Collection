defaults:
  - basis
  - managers: auto_atom/basis
  - demonstrators: auto_atom/basis
  - samplers: ros_struct
  - _self_
  - override hydra/job_logging: custom
  - override hydra/launcher: joblib  # or `add hydra/launcher=joblib` to the command line

visualizer: null

managers:
  auto_atom:
    env:
      viewer: null
