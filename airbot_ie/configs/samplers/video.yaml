# @package _global_

sampler:
  _target_: airdc.common.samplers.video_sampler.VideoSampler
  remove_mode: trash
  # encoder:
  #   cfg:
  #     non_monotonic_mode: drop
  #     non_monotonic_log: false

dataset:
  directory: video
  file_extension: ""
