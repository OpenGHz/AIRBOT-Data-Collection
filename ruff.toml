[lint]
ignore = ["F841", "F401"]
exclude = [
    "airdc/common/utils/transformations.py",
    # Demo / manual scripts are exempt from linting; real tests are linted.
    "tests/manual/*",
    "airbot_ie/tests/manual/*",
]
